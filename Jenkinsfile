pipeline {
    agent any
        stages {
            stage ('Build') {steps{echo 'Stage1' }}
                stage ('Build1') {steps{echo 'Stage1' }}
                stage ('Build2') {steps{echo 'Stage1' }}
                stage ('Build3') {steps{echo 'Stage1' }}
              }
       }
